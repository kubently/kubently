# Generate a strong Redis password and save it here
# Example: openssl rand -base64 32 > secrets/redis-password.txt
# Then apply: kubectl create secret generic kubently-redis-password --from-file=password=secrets/redis-password.txt -n kubently
